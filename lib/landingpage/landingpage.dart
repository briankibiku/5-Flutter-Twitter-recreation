import 'package:flutter/material.dart';

class LandingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      appBar: AppBar(
        title: Text('Twitter'),
        backgroundColor: Color(0xFF1ba1f3),
      ),
      body: Container(
        child: Column(children: <Widget>[],),
      ),
    );
  }
}
